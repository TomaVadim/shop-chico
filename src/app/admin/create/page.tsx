import { SectionWrapper } from "@/features/components/section-wrapper/section-wrapper";
import { FormCreateNewProduct } from "./form";

export default function Create() {
  return (
    <SectionWrapper>
      <FormCreateNewProduct />
    </SectionWrapper>
  );
}
